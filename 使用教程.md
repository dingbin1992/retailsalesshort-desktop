# 流向整理工具 v3.1 — 使用教程

## 一、工具简介

**流向整理工具**是一款桌面应用程序（基于 Tauri v2），用于将湖北区域各医药公司每日网上下载的出库 Excel 文件，自动汇总整理为标准格式的 Excel 汇总表，并自动填充统计基表中的计算公式。

### 核心功能

1. **自动识别**：通过表头列名自动匹配 12 种 Excel 格式，无需手动指定
2. **批量汇总**：将多个药企的出库文件合并为一份汇总表
3. **自动填充公式**：将数据写入月度统计基表，自动填充 14 列计算公式
4. **实时反馈**：进度条 + 日志区，处理过程一目了然
5. **自动清理**：处理成功后自动删除工作目录中的原始文件

### 支持识别的商业单位

| 商业单位 | 对应格式 |
|---|---|
| 宜昌市瑞康医药有限责任公司 | pattern1 |
| 九州通医药集团股份有限公司 | pattern2、pattern10 |
| 湖北人福医药集团有限公司 | pattern3 |
| 湖北华富康药业有限公司 | pattern4 |
| 上药控股湖北有限公司 | pattern5 |
| 南京医药湖北有限公司 | pattern6、pattern12 |
| 华润湖北医药有限公司 | pattern7 |
| 国药控股湖北有限公司 | pattern8 |
| 荆门市医药药材有限公司 | pattern9、pattern11 |

---

## 二、安装与环境要求

### 系统要求

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10+ / macOS 10.15+ / Linux |
| 基表路径 | `H:\0、工作\0、每日纯销统计\☆☆纯销统计基表.xlsx` 必须存在 |
| 磁盘空间 | 100 MB 以上 |

### 安装方式

**方式一：直接运行安装包**

双击 `流向整理工具_3.1.0_x64-setup.exe`（Windows）或 `.dmg`（macOS）安装。

**方式二：从源码运行（开发者）**

```bash
git clone <仓库地址>
cd retailsalesshort-desktop
npm install
npm run download:node     # 首次使用需下载便携版 Node.js
npm run build             # 编译数据处理模块
npm run tauri:dev         # 开发模式运行
```

---

## 三、界面说明

启动程序后，你会看到以下界面：

### 3.1 选择年月

自动生成 2024 年至下一年度的年份选项，以及 1~12 月选项。默认选中当前年月。

### 3.2 工作目录

存放各医药公司原始下载 Excel 文件（`.xls` / `.xlsx`）的目录。点击右侧 **浏览...** 按钮选择。

### 3.3 输出目录

汇总文件和统计表保存的位置。点击右侧 **浏览...** 按钮选择。

### 3.4 操作面板

- **开始处理**（紫色按钮）：启动自动整理流程
- **修改配置**（橙色按钮）：用系统默认程序打开格式配置文件
- **进度条**：处理过程中显示实时进度百分比

### 3.5 未匹配文件提示

如果存在格式无法识别的 Excel 文件，会在处理完成后以橙色面板显示文件名列表。

### 3.6 处理日志

深色区域实时显示处理日志，不同类型用不同颜色区分：

| 颜色 | 含义 |
|---|---|
| 紫色 | 普通信息 |
| 绿色 | 成功/写入完成 |
| 橙色 | 错误/警告 |

---

## 四、日常使用流程

### 步骤 1：准备文件

将各商业单位从网上下载的当日出库 Excel 文件放入同一个文件夹中。

> **注意**：文件名可以任意，程序通过表头识别格式，不依赖文件名。

### 步骤 2：打开工具并设置参数

1. 选择需要处理的**年份**和**月份**
2. 点击**浏览...** 选择工作目录（存放原始文件的目录）
3. 点击**浏览...** 选择输出目录（汇总文件保存位置）

### 步骤 3：开始处理

点击**开始处理**按钮，程序按以下顺序自动执行：

1. **扫描文件**：统计工作目录中所有 `.xls` / `.xlsx` 文件，计算总数据行数
2. **创建汇总文件**：生成 `湖北区域每日网上下载出库汇总YYYY-M-D.xlsx`
3. **逐个处理文件**：
   - 读取 Excel 文件
   - 比对表头列名，匹配格式规则（patterns.json）
   - 提取关键数据列（日期、品种、规格、批号、流向单位、数量）
   - 追加到汇总文件
4. **写入目标表**：将汇总数据写入 `湖北纯销每日统计XX年XX月.xlsx` 的"纯销明细"工作表
   - A~G 列：粘贴原始数据
   - H~U 列：自动填充计算公式（销售额、区域归属、审核标记等）
5. **清理工作目录**：删除已处理的原始 Excel 文件
6. **自动打开结果**：处理完成后自动打开汇总文件所在目录

### 步骤 4：查看结果

处理完成后会自动打开输出目录，你可以看到：

- **汇总文件**：`湖北区域每日网上下载出库汇总YYYY-M-D.xlsx`（日期为前一天）
- **月度统计表**：`湖北纯销每日统计XX年XX月.xlsx`（位于 `H:\0、工作\0、每日纯销统计\XX年XX月\`）

汇总表包含以下列：

| 列 | 内容 |
|---|---|
| A | 商业单位 |
| B | 日期 |
| C | 品种 |
| D | 规格 |
| E | 批号 |
| F | 流向单位 |
| G | 数量 |

---

## 五、配置管理

### 5.1 格式规则文件（patterns.json）

格式规则文件位于程序目录的 `config/patterns.json`，它定义了如何识别各商业单位的 Excel 文件。

**规则结构示例：**

```json
{
  "name": "pattern1",
  "businessUnit": "宜昌市瑞康医药有限责任公司",
  "headers": {
    "1": "销售日期",
    "2": "单位名称",
    "3": "商品名称",
    "4": "规格",
    "5": "生产厂家",
    "6": "批号",
    "7": "购货单位",
    "8": "数量"
  },
  "mapping": {
    "1": 1,
    "3": 2,
    "4": 3,
    "6": 4,
    "7": 5,
    "8": 6
  }
}
```

**字段说明：**

| 字段 | 说明 |
|---|---|
| `name` | 格式名称（唯一标识，如 `pattern1`） |
| `businessUnit` | 商业单位全称（会写入汇总表 A 列和统计表） |
| `headers` | Excel 列号 → 列名映射（用于**识别**该文件属于哪个商业单位） |
| `mapping` | 源列号 → 目标列号映射（用于**提取**数据） |

**目标列号含义：**

| 目标列号 | 数据内容 |
|---|---|
| 1 | 日期 |
| 2 | 品种 |
| 3 | 规格 |
| 4 | 批号 |
| 5 | 流向单位 |
| 6 | 数量 |

### 5.2 修改配置

点击界面上的**修改配置**按钮，程序会用系统默认文本编辑器打开 `patterns.json`。

### 5.3 添加新的商业单位格式

当遇到新的商业单位或表头格式变化时：

1. 打开该单位的 Excel 文件，查看**表头行**的列名
2. 确定每一列对应的列号（Excel 中 A 列=1，B 列=2，以此类推）
3. 在 `patterns.json` 的 `"patterns"` 数组中新增一条规则
4. 保存文件后**重新点击"开始处理"**即可生效（无需重启程序）

**示例：新增"某某医药公司"**

```json
{
  "name": "pattern13",
  "businessUnit": "某某医药有限公司",
  "headers": {
    "1": "日期",
    "2": "销往单位",
    "3": "药品名称",
    "4": "规格",
    "5": "批号",
    "6": "数量"
  },
  "mapping": {
    "1": 1,
    "3": 2,
    "4": 3,
    "5": 4,
    "2": 5,
    "6": 6
  }
}
```

---

## 六、常见问题

### Q1：程序启动报错"无法加载 patterns.json 配置文件"

**原因**：配置文件缺失或路径不正确。

**解决**：检查 `config/patterns.json` 是否存在。如果是首次使用，将 `config/patterns_template.json` 复制并重命名为 `patterns.json`。

### Q2：文件被跳过，提示"未识别的文件格式"

**原因**：该 Excel 文件的表头列名与 `patterns.json` 中所有已知格式规则都不匹配。

**解决**：
1. 打开被跳过的 Excel 文件，查看表头行的列名
2. 参考第五节"添加新的商业单位格式"，新增对应的匹配规则
3. 将未匹配的文件重新放入工作目录，再次点击"开始处理"

### Q3：提示"基表文件不存在"

**原因**：程序依赖的统计基表文件 `H:\0、工作\0、每日纯销统计\☆☆纯销统计基表.xlsx` 不存在或路径无法访问。

**解决**：确认该文件存在于指定路径。此路径当前在程序中硬编码，如需修改请联系开发人员。

### Q4：处理完成后数据比预期少

**原因**：通常有两种情况：
- 某些文件格式未匹配导致被整体跳过（查看未匹配文件面板）
- Excel 文件中的空行会被自动过滤（属于正常行为）

**解决**：先检查日志中是否有格式不匹配的提示，再查看处理行数统计。

### Q5：处理完成后工作目录中的文件不见了

**这是正常行为**。程序处理成功后会自动**清理工作目录**，删除已处理的原始 Excel 文件，避免下次重复处理。如果你需要保留原始文件，请提前备份。

### Q6：日志区出现红色错误信息

说明处理过程中出现了异常。请查看具体错误内容：

- **格式不匹配**：参考 Q2 解决
- **文件被占用**：关闭正在打开的 Excel 文件后重试
- **路径不存在**：检查工作目录和输出目录是否正确

### Q7：可以同时处理多个文件吗？

可以。程序会自动扫描工作目录中**所有** `.xls` 和 `.xlsx` 文件，逐文件识别格式并提取数据，合并到同一份汇总表中。处理过程有进度条和日志实时反馈。

---

## 七、处理流程图

```
┌─────────────────────────────────────────────────┐
│ 1. 选择年月、工作目录、输出目录                    │
└────────────────────┬────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────┐
│ 2. 扫描工作目录 → 列出所有 .xls/.xlsx 文件         │
│    统计每个文件的行数，计算总行数                    │
└────────────────────┬────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────┐
│ 3. 创建汇总文件                                   │
│    湖北区域每日网上下载出库汇总YYYY-M-D.xlsx        │
└────────────────────┬────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────┐
│ 4. 逐个文件处理                                   │
│    ┌─ 读取 Excel 文件                             │
│    ├─ 比对表头 → 匹配格式规则                      │
│    ├─ 匹配成功 → 提取数据 → 追加到汇总文件          │
│    └─ 匹配失败 → 记入未匹配列表，跳过              │
└────────────────────┬────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────┐
│ 5. 写入月度统计表                                │
│    湖北纯销每日统计XX年XX月.xlsx                   │
│    ├─ A~G列：粘贴原始数据                          │
│    └─ H~U列：自动填充计算公式                      │
└────────────────────┬────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────┐
│ 6. 清理工作目录 → 删除已处理文件                    │
│    自动打开输出目录                                │
└─────────────────────────────────────────────────┘
```

---

## 八、技术支持

- **版本**：v3.1.0
- **技术栈**：Tauri v2 + Vue 3 + Node.js
- **配置文件**：`config/patterns.json`
- **日志文件**：程序运行日志位于系统临时目录
