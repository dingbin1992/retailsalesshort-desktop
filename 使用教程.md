# 流向整理工具 — 使用教程

## 一、工具简介

**流向整理工具**是一款桌面应用程序，用于将湖北区域各医药公司每日网上下载的出库 Excel 文件，自动汇总为一份标准格式的 Excel 汇总表。

**支持识别的商业单位：**

| 商业单位 | 对应格式 |
|---|---|
| 宜昌市瑞康医药有限责任公司 | pattern1 |
| 九州通医药集团股份有限公司 | pattern2、pattern10 |
| 湖北人福医药集团有限公司 | pattern3 |
| 湖北华富康药业有限公司 | pattern4 |
| 上药控股湖北有限公司 | pattern5 |
| 南京医药湖北有限公司 | pattern6、pattern12 |
| 华润湖北医药有限公司 | pattern7 |
| 国药控股湖北有限公司 | pattern8 |
| 荆门市医药药材有限公司 | pattern9、pattern11 |

## 二、首次使用 — 初始化配置（重要）

在首次使用前，需要执行以下初始化步骤：

### 步骤 1：重命名配置文件

进入项目 `config` 文件夹，将 `patterns_template.json` 重命名为 `patterns.json`：

```
config/
  patterns_template.json  →  patterns.json   ← 重命名此文件
```

**为什么要这样做？** 程序启动时会自动加载 `config/patterns.json` 来读取各商业单位的 Excel 格式识别规则。如果没有这个文件，程序将无法启动。

### 步骤 2：检查配置文件内容

用文本编辑器打开 `config/patterns.json`，确认其中已包含你所需要的商业单位格式规则。每个格式规则的结构如下：

```json
{
  "name": "pattern1",
  "businessUnit": "宜昌市瑞康医药有限责任公司",
  "headers": {
    "1": "销售日期",
    "2": "单位名称",
    "3": "商品名称",
    ...
  },
  "mapping": {
    "1": 1,
    "3": 2,
    ...
  }
}
```

- `name`：格式名称（唯一标识）
- `businessUnit`：商业单位名称（会写入汇总表第一列）
- `headers`：Excel 表头列号与列名的对应关系（用于识别文件属于哪个商业单位）
- `mapping`：源列号到目标列号的映射（`1=日期, 2=品种, 3=规格, 4=批号, 5=流向单位, 6=数量`）

### 步骤 3：启动程序

```bash
# 开发模式启动
npm run start

# 或打包后运行
npm run dist:win
```

## 三、日常使用流程

### 1. 准备工作目录

将各商业单位下载的 Excel 出库文件（`.xls` 或 `.xlsx`）放入同一个文件夹中，作为**工作目录**。

### 2. 打开工具并选择目录

启动程序后，你会看到如下界面：

- **工作目录**：点击"浏览..."选择存放原始 Excel 文件的目录
- **输出目录**：点击"浏览..."选择汇总文件的保存位置（建议选择桌面）

> 目录选择后会自动保存，下次打开无需重新选择。

### 3. 点击"开始处理"

点击**开始处理**按钮，程序将自动执行以下流程：

1. 扫描工作目录中的所有 Excel 文件
2. 统计每个文件的数据行数
3. 逐个识别文件格式（匹配表头列名）
4. 提取关键列数据（日期、品种、规格、批号、流向单位、数量）
5. 追加到汇总文件

进度条会实时显示处理进度，日志区会输出详细处理信息。

### 4. 查看结果

处理完成后，程序会自动打开输出目录。汇总文件命名格式为：

```
湖北区域每日网上下载出库汇总YYYY-M-D.xlsx
```

（日期为前一天）

汇总表包含以下列：

| 商业单位 | 日期 | 品种 | 规格 | 批号 | 流向单位 | 数量 |
|---|---|---|---|---|---|---|

## 四、添加新的商业单位格式

如果遇到新的商业单位 Excel 格式，需要添加识别规则：

### 方式一：直接在工具中打开配置文件

点击界面上的**修改配置**按钮，程序会用默认文本编辑器打开 `patterns.json`。

### 方式二：手动编辑配置文件

用文本编辑器打开 `config/patterns.json`，在 `"patterns"` 数组中新增一条格式规则。

**新增规则步骤：**

1. 打开该商业单位的 Excel 文件，查看表头行的列名
2. 对照表头确认每一列的位置，例如：

```
列1: 日期 → 目标列1
列3: 药品名称 → 目标列2
列5: 规格 → 目标列3
列8: 批号 → 目标列4
列2: 销往单位 → 目标列5
列6: 数量 → 目标列6
```

3. 在 `patterns` 数组中添加新条目：

```json
{
  "name": "pattern_新单位",
  "businessUnit": "某某医药公司",
  "headers": {
    "1": "日期",
    "3": "药品名称",
    "5": "规格",
    "8": "批号",
    "2": "销往单位",
    "6": "数量"
  },
  "mapping": {
    "1": 1,
    "3": 2,
    "5": 3,
    "8": 4,
    "2": 5,
    "6": 6
  }
}
```

4. 保存文件并重启程序

## 五、常见问题

### Q1: 程序启动报错"无法加载 patterns.json 配置文件"

**原因**：未执行初始化步骤，缺少 `config/patterns.json` 文件。

**解决**：将 `config/patterns_template.json` 重命名为 `config/patterns.json`。

### Q2: 文件被跳过，提示"未识别的文件格式"

**原因**：该 Excel 文件的表头列名与已知的所有格式规则都不匹配。

**解决**：
1. 打开被跳过的 Excel 文件，查看表头行的列名
2. 参考第四节"添加新的商业单位格式"，新增对应的匹配规则
3. 将未匹配的文件重新放入工作目录，再次点击"开始处理"

### Q3: 处理完成后部分数据未被处理

日志会显示处理率，如果低于 100%，可能是某些行数据为空行（被自动跳过），这不影响正常使用。

### Q4: 汇总文件中的数据比源文件少

检查日志中是否有报错信息，通常原因是格式未匹配导致整文件跳过。

### Q5: 需要处理的文件很多，程序会卡住吗？

程序采用分批处理机制（每批 1000 行数据），日志区会实时更新进度，界面不会卡死。

## 六、目录结构说明

```
retailsalesshort-desktop/
├── config/
│   ├── patterns_template.json   ← 格式模板（初始化时重命名为 patterns.json）
│   └── patterns.json            ← 实际使用的格式配置（需从模板复制）
├── src/
│   ├── main/                    ← Electron 主进程代码
│   ├── renderer/                ← 界面代码
│   └── shared/                  ← 共享类型定义
├── release/                     ← 打包输出目录
└── package.json
```
