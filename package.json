{
  "name": "retailsalesshort-desktop",
  "version": "3.2.0",
  "description": "湖北区域每日网上下载出库汇总工具",
  "scripts": {
    "build": "esbuild src/processing/cli.ts --bundle --platform=node --target=node20 --outfile=dist/processing/cli.js && esbuild src/processing/read-headers.ts --bundle --platform=node --target=node20 --outfile=dist/processing/read-headers.js && node scripts/copy-tauri-api.js",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:build:win": "tauri build --target x86_64-pc-windows-msvc",
    "tauri:build:mac": "tauri build --target aarch64-apple-darwin",
    "tauri:build:mac:x64": "tauri build --target x86_64-apple-darwin",
    "tauri:build:linux": "tauri build --target x86_64-unknown-linux-gnu",
    "download:node": "node scripts/download-node.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2",
    "@types/node": "^25.8.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "exceljs": "^4.4.0",
    "xlsx": "^0.18.5"
  }
}
